# Development Environment Configuration
ENVIRONMENT=development
DEBUG_MODE=true

# Security (development keys - never use in production)
SECRET_KEY=dev-secret-key-not-secure
ALGORITHM=HS256

# JWT Configuration (longer tokens for development convenience)
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=7

# Database
DATABASE_URL=sqlite:///./totus_tuus.db

# CORS - Allow local development
BACKEND_CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173

# API Configuration
API_V1_STR=/api/v1
PROJECT_NAME=Totus Tuus - App de Consagración Total (DEV)

# Rate Limiting (relaxed for development)
PROGRESS_RATE_LIMIT=100
AUTH_RATE_LIMIT=50
GENERAL_RATE_LIMIT=500
LIBRE_MODE_RATE_LIMIT=10